#include<stdio.h>
int main(){
    int x=2;

//x=1 replaces 2 from the memory and assign a new value of x , if we want to check that weather x is equal to 1 or not then we will use x==1, if this is true it will print if statement if not then print else statement
    if (x=1){
        printf("x is equal to 1 ");
    }else{
        printf("x is not equal to 1");
    }
    return 0;
}